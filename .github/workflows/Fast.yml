name: Fast Build ReleaseS

on:
  push:
    branches: [ main ]
    paths:
      - '**'
      - '!**/assets/**'
      - '!**.md'
      - '!**/ISSUE_TEMPLATE/**'
      - '!**/modules/web/**'
  pull_request:
    paths-ignore:
      - '**/modules/web/**'
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  prepare:
    runs-on: ubuntu-latest
    if: github.event_name != 'pull_request'
    outputs:
      version: ${{ steps.set-ver.outputs.version }}
      versionL: ${{ steps.set-ver.outputs.versionL }}
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 1
      - id: set-ver
        run: |
          echo "version=$(date -d "8 hour" -u +3.%y.%m%d%H)" >> $GITHUB_OUTPUT
          echo "versionL=$(date -d "8 hour" -u +3.%y.%m%d%H)" >> $GITHUB_OUTPUT

  build:
    needs: prepare
    runs-on: ubuntu-latest
    if: github.event_name != 'pull_request'
    env:
      VERSION: ${{ needs.prepare.outputs.version }}
      VERSIONL: ${{ needs.prepare.outputs.versionL }}
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 1

      - name: Set up JDK 17
        uses: actions/setup-java@v5
        with:
          distribution: 'temurin'
          java-version: 17

      - name: Sign config
        run: |
          cp .github/workflows/legado.jks app/legado.jks
          cat >> gradle.properties << EOF
          RELEASE_STORE_FILE=./legado.jks
          RELEASE_KEY_ALIAS=legado
          RELEASE_STORE_PASSWORD=gedoor_legado
          RELEASE_KEY_PASSWORD=gedoor_legado
          EOF

      - name: Optimize Gradle settings for fast debug
        run: |
          cat >> gradle.properties << 'EOF'
          org.gradle.parallel=true
          org.gradle.caching=true
          org.gradle.configuration-cache=false
          org.gradle.jvmargs=-Xmx4096m -XX:MaxMetaspaceSize=512m -XX:+UseParallelGC
          org.gradle.configureondemand=true
          org.gradle.workers.max=4
          kotlin.incremental=false
          android.enableLint=false
          EOF

      - uses: gradle/actions/setup-gradle@v6
        with:
          gradle-version: wrapper
          cache-read-only: false

      - name: Disable minify & shrink
        run: |
          sed -i 's/minifyEnabled.*/minifyEnabled false/g' app/build.gradle
          sed -i 's/shrinkResources.*/shrinkResources false/g' app/build.gradle

      - name: Set version
        run: |
          sed -i "/def version/c def version = \"$VERSION\"" app/build.gradle

      - name: Build releaseS APK
        run: |
          chmod +x gradlew
          ./gradlew assembleAppSRelease \
            --no-daemon \
            --parallel \
            --max-workers=4 \
            -x test \
            -x lint \
            -x check \
            --warning-mode none

      - name: Collect APK
        run: |
          mkdir -p apk
          find app/build/outputs/apk -name "*.apk" -exec mv {} apk/legado_正式版_${{ env.VERSIONL }}_releaseS.apk \;

      - name: Upload APK
        uses: actions/upload-artifact@v7
        with:
          name: legado.releaseS.apk
          path: apk/*.apk
